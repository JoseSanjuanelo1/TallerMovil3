<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.taller3movil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Areas" />
        <activity android:name=".ACuadrado" />
        <activity android:name=".resultadoCuadrado" />
        <activity android:name=".Rectangulo" />
        <activity android:name=".ResultadoRectangulo" />
        <activity android:name=".areas2" />
        <activity android:name=".listado" />
    </application>

</manifest>